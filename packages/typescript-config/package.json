{
	"name": "@repo/typescript-config",
	"version": "0.0.0",
	"private": true,
	"license": "MIT",
	"scripts": {
		"build": "echo 'No build needed for TypeScript config'",
		"check-types": "echo 'No type checking needed for TypeScript config'",
		"lint": "echo 'No linting needed for TypeScript config'",
		"clean": "echo 'No cleanup needed for TypeScript config'"
	},
	"files": [
		"base.json",
		"nextjs.json",
		"astro.json"
	]
}
