<script>
	import { actions } from "astro:actions"
	import { navigate } from "astro:transitions/client"

	let params = new URLSearchParams(document.location.search)
	let redirect = params.get("redirect")
	const redirectUrl = new URL(redirect || "/", document.location.origin)
	console.log("redirect", redirectUrl)

	const form = document.querySelector("form")
	form?.addEventListener("submit", async (event) => {
		event.preventDefault()
		const formData = new FormData(form)
		const { data, error } = await actions.login(formData)
		if (!error) {
			console.log("redirecting to", redirect)
			navigate(redirectUrl.href)
		}
	})
</script>

<div class="flex h-[50svh] w-full gap-4">
	<div class="flex basis-1/2 items-center justify-center">
		<h1
			class="font-poppins text-[50px] font-bold uppercase leading-none lg:text-[75px] xl:text-[100px]"
		>
			Dein Account
		</h1>
	</div>
	<div class="flex basis-1/2 items-center justify-center bg-white/30">
		<form>
			<div class="mb-4 flex flex-col items-start gap-1">
				<label for="email" class="">Email</label>
				<input
					type="email"
					id="email"
					name="email"
					class="rounded-lg px-4 py-1"
					required
				/>
			</div>
			<div class="mb-6 flex flex-col items-start gap-1">
				<label for="password" class="">Password</label>
				<input
					type="password"
					id="password"
					name="password"
					class="rounded-lg px-4 py-1"
					required
				/>
			</div>
			<div class="flex items-center justify-between">
				<button
					type="submit"
					class="focus:shadow-outline rounded bg-blue-500 px-4 py-2 font-bold text-white hover:bg-blue-700 focus:outline-none"
					>Login</button
				>
			</div>
		</form>
	</div>
</div>
